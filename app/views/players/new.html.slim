h2 Player Registration

- if @signup_closed
  p.flash-error Signup is currently closed.
- else
  = simple_form_for @player do |f|
    = f.input :name, label: 'Full Name'
    = f.input :walker
    = f.input :rider
    = f.input :on_standby, as: :hidden
    = f.button :submit, "Register"
